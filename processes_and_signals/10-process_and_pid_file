#!/usr/bin/env bash
# asdadasd

echo "$$" > /var/run/myscript.pid

trap 'echo I hate the kill command; rm -rf /var/run/myscript.pid' TERM
trap 'echo Y U no love me?!' INT
trap 'rm -rf /var/run/myscript.pid' QUIT

while true; do
	echo "To infinity and beyond"
	sleep 2
done
