#!/usr/bin/env bash
# asdadasd

PIDFILE="/var/run/myscript.pid"

echo "$!" > "$PIDFILE"

trap 'echo I hate the kill command; rm -rf "$PIDFILE"' TERM
trap 'echo Y U no love me?!' INT
trap 'rm -rf "$PIDFILE"' QUIT

while true; do
	echo "To infinity and beyond"
	sleep 2
done
